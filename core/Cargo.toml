[package]
name = "legajo-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["time", "sync", "macros", "rt-multi-thread", "process", "io-util", "fs"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "gzip"] }
url = "2"
thiserror = "2"
chrono = { version = "0.4", features = ["serde"] }
rusqlite = { version = "0.32", features = ["bundled"] }
# Para instalar la capa de ejecucion del extractor: verificar la huella del
# interprete y desempaquetarlo. Ver `entorno.rs`.
sha2 = "0.10"
flate2 = "1"
tar = "0.4"
