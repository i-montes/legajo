# Torch en CPU a propósito: la extracción tiene que medirse en la máquina que
# va a tener una redacción, no en una con GPU. La rueda de CPU pesa una
# fracción de la de CUDA y es lo que se acabará empaquetando.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0

# spaCy segmenta oraciones y tokeniza. Es lo que le da a GLiNER trozos con
# sentido gramatical en vez de ventanas de N palabras, y lo que permite alinear
# las entidades a límites de token antes de pasárselas a GLiREL.
spacy>=3.8
es-core-news-sm @ https://github.com/explosion/spacy-models/releases/download/es_core_news_sm-3.8.0/es_core_news_sm-3.8.0-py3-none-any.whl

# Entidades y relaciones, ambos de vocabulario abierto.
gliner>=0.2.13
glirel>=1.0.0
# glirel 1.2.1 importa loguru sin declararlo entre sus dependencias.
loguru
