# Torch en CPU a propósito: la extracción tiene que medirse en la máquina que
# va a tener una redacción, no en una con GPU. La rueda de CPU pesa una
# fracción de la de CUDA y es lo que se acabará empaquetando.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0

# spaCy segmenta oraciones. Es lo que le da al modelo trozos con sentido
# gramatical en vez de ventanas de N palabras: una entidad partida por la mitad
# no da error, simplemente no aparece.
spacy>=3.8
es-core-news-sm @ https://github.com/explosion/spacy-models/releases/download/es_core_news_sm-3.8.0/es_core_news_sm-3.8.0-py3-none-any.whl

# Entidades y relaciones en una sola pasada, de vocabulario abierto. La familia
# relex —entidades y relaciones juntas— entró en gliner 0.2.28.
gliner>=0.2.28
